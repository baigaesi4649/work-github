<h1>商品一覧</h1>
<%= link_to "+",new_admin_item_path %>
<% @items.each do |item| %>
<div>
  <p><%= item.id %></p>
  <p><%= item.name %></p>
  <p><%= item.price %></p>
  <p><%= item.genre_id %></p>
  <p><%= item.is_active %></p>
</div>
<% end %>